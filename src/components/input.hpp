#pragma once

struct input {
    /* data */
    bool upKeyPressed;
    bool downKeyPressed;
    bool leftKeyPressed;
    bool rightKeyPressed;
    bool resetKeyPressed;
    double xpos;
    double ypos;
};

#pragma once

struct inputKeyboard
{
    /* data */
    bool upKeyPressed;
    bool downKeyPressed;
    bool leftKeyPressed;
    bool rightKeyPressed;
    bool resetKeyPressed;
    bool loadKeyPressed;
    bool saveKeyPressed;
};
